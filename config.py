# Shared folder on M3. Use for anything that would be needed if someone else runs the code.
sharedFolder = "/work/group/egodat/reu23_clark/temp/"

# Put all datasets in here. The data should not be on GitHub (because of size) but should be easy to get.
dataFolder = sharedFolder + "data/"
